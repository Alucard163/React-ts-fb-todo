import { FETCH_TODOS } from "./types";

// export const fetchToDos = (id: any) => async dispatch => {
//     todosRef.child(id).on("value", snapshot => {
//         dispatch({
//             type: FETCH_TODOS,
//             payload: snapshot.val()
//         });
//     });
// };